[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
per-file-ignores = { "scripts/*.py" = ["E402"], "frontend/attendance_app.py" = ["E402"] }

[tool.ruff.format]
quote-style = "preserve"

[tool.pytest.ini_options]
testpaths = ["tests"]
